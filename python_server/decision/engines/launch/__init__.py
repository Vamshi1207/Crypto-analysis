"""Launch paper book — young Gecko pools before a full cluster forms.

Scan loop lives in ``decision.trenches`` next to cluster (same Gecko pass).
"""

LANE_ID = "launch"
LABEL = "Launch"
STRATEGY = "launch"
