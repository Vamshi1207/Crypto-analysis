[pytest]
testpaths = tests
# The server modules are top-level in /app, not an installed package.
pythonpath = .
# Default suite stays offline: live Gate 0 / network tests opt in with `-m live`.
addopts = -q -m "not live"
